mutation UpdateProfile($input: UpdateUserInput!) {
    updateProfileInfo(user: $input) {
        success
        accessToken
        refreshToken
        user {
            id
            email
            firstName
            lastName
            profileImageUrl
            refreshToken
        }
    }
}