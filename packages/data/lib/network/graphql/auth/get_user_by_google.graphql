query GetUserByGoogleId($googleId: String!) {
  getUserByGoogleId(googleId: $googleId) {
    success
    user{
      id
      email
      phoneNumber
      googleId
      username
      firstName
    }
  }
}